[project]
name = "featureform"
version = "0.1.0"
description = "Add a short description here"
authors = ["egorgorban <noreply.egorgorban@inbox.ru>"]
channels = ["conda-forge", "bioconda"]
platforms = ["linux-64"]

[tasks]

[dependencies]
python = ">=3.12.3,<3.13"
googleapis-common-protos = ">=1.63.0,<1.64"

# [pypi-dependencies]
# featureform = { git = "ssh://git@github.com/featureform/featureform" } # pip install featureform

[feature.dev.dependencies]
isort = ">=5.6.2,<5.14"
ruff = ">=0.4.5,<0.5"
ipykernel = ">=6.14.0,<6.30"

[environments]
dev = {features = ["dev"]}
